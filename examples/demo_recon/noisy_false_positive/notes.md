# Noisy False Positive Notes

- Several hostnames look sensitive by name, including admin, dev, backup, and old admin labels.
- The captured metadata shows authentication, forbidden, not found, or gone responses for several of these. That should temper any later language.
- `backup.zip` appears in historical-looking URLs, but the dataset does not demonstrate access to a backup file.
- Redirect-like parameters appear in multiple places and should be recorded neutrally for manual review.
- This sample is meant to discourage simplistic keyword-only triage.
